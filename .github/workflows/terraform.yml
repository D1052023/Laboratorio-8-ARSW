name: Terraform CI/CD

on:
  pull_request:
  workflow_dispatch:

jobs:
  terraform:
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Azure Login (OIDC)
      uses: azure/login@v1
      with:
        client-id: ${{ secrets.AZURE_CLIENT_ID }}
        tenant-id: ${{ secrets.AZURE_TENANT_ID }}
        subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v2

    # ✅ TODO usando el mismo contexto (infra)
    - name: Terraform Init
      run: terraform -chdir=infra init -backend-config=backend.hcl

    - name: Terraform Format
      run: terraform -chdir=infra fmt -recursive

    - name: Terraform Validate
      run: terraform -chdir=infra validate

    - name: Terraform Plan
      run: terraform -chdir=infra plan -var-file=env/dev.tfvars

  apply:
    needs: terraform
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'

    permissions:
      id-token: write
      contents: read

    steps:
    - uses: actions/checkout@v3

    - uses: azure/login@v1
      with:
        client-id: ${{ secrets.AZURE_CLIENT_ID }}
        tenant-id: ${{ secrets.AZURE_TENANT_ID }}
        subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

    - uses: hashicorp/setup-terraform@v2

    - name: Terraform Init
      run: terraform -chdir=infra init -backend-config=backend.hcl

    - name: Terraform Apply
      run: terraform -chdir=infra apply -var-file=env/dev.tfvars -auto-approve
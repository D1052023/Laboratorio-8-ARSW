name: Terraform CI/CD

on:
  pull_request:
  workflow_dispatch:

jobs:
  terraform:
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Azure Login (OIDC)
      uses: azure/login@v1
      with:
        client-id: ${{ secrets.AZURE_CLIENT_ID }}
        tenant-id: ${{ secrets.AZURE_TENANT_ID }}
        subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v2

    # 🔥 INIT CORREGIDO (clave)
    - name: Terraform Init
      run: terraform -chdir=infra init -backend-config=infra/backend.hcl

    - name: Terraform Format
      run: terraform -chdir=infra fmt -recursive

    - name: Terraform Validate
      run: terraform -chdir=infra validate

    # 🔥 PLAN CORRECTO
    - name: Terraform Plan
      run: terraform -chdir=infra plan -var-file=env/dev.tfvars

  apply:
    needs: terraform
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'

    permissions:
      id-token: write
      contents: read

    steps:
    - uses: actions/checkout@v3

    - uses: azure/login@v1
      with:
        client-id: ${{ secrets.AZURE_CLIENT_ID }}
        tenant-id: ${{ secrets.AZURE_TENANT_ID }}
        subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

    - uses: hashicorp/setup-terraform@v2

    # 🔥 MISMO INIT
    - name: Terraform Init
      run: terraform -chdir=infra init -backend-config=infra/backend.hcl

    # 🔥 APPLY CORRECTO
    - name: Terraform Apply
      run: terraform -chdir=infra apply -var-file=env/dev.tfvars -auto-approve
name: Terraform CI/CD

on:
  pull_request:
  workflow_dispatch:

jobs:
  terraform:
    runs-on: ubuntu-latest
    # 💡 Esto hace que todos los 'run' de este job se ejecuten en 'infra' por defecto
    defaults:
      run:
        working-directory: infra

    permissions:
      id-token: write
      contents: read

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Azure Login (OIDC)
      uses: azure/login@v1
      with:
        client-id: ${{ secrets.AZURE_CLIENT_ID }}
        tenant-id: ${{ secrets.AZURE_TENANT_ID }}
        subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v2

    - name: Terraform Init
      # Ahora el path al archivo hcl es relativo a la carpeta 'infra'
      run: terraform init -backend-config=backend.hcl

    - name: Terraform Format
      run: terraform fmt -recursive

    - name: Terraform Validate
      run: terraform validate

    - name: Terraform Plan
      # Ya no necesitas el 'cd infra'
      run: terraform plan -var-file=env/dev.tfvars

  apply:
    needs: terraform
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    defaults:
      run:
        working-directory: infra

    permissions:
      id-token: write
      contents: read

    steps:
    - uses: actions/checkout@v3

    - uses: azure/login@v1
      with:
        client-id: ${{ secrets.AZURE_CLIENT_ID }}
        tenant-id: ${{ secrets.AZURE_TENANT_ID }}
        subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

    - uses: hashicorp/setup-terraform@v2

    - name: Terraform Init
      run: terraform init -backend-config=backend.hcl

    - name: Terraform Apply
      run: terraform apply -var-file=env/dev.tfvars -auto-approve